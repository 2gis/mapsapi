language: node_js
node_js:
  - "0.10"

before_install:
  - npm install -g gulp

install:
  - npm install

before_script:
  - gulp build

script:
  - gulp test

notifications:
  email:
    recipients:
      - jsapi@2gis.ru
    on_success: [never] # default: change
    on_failure: [always] # default: always
