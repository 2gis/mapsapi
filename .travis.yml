language: node_js
sudo: false
node_js:
  - "0.10"

before_install:
  - npm install -g gulp

install:
  - npm install

script:
  - gulp test

after_success: cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage
