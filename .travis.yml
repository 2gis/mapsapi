language: node_js
node_js:
  - "0.10"

before_install:
  - npm install -g gulp

install:
  - npm install

script:
  - gulp test

notifications:
  email:
    recipients:
      - onikiienko@gmail.com
      - jsapi@2gis.ru
    on_success: [always] # default: change
    on_failure: [always] # default: always
