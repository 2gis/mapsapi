// describe('DG.Control.Ruler', function () {
// 	it('control.ruler factory creates Control.Ruler instance', function () {
// 		var options = { position: 'bottomleft' };
// 		expect(L.DG.Control.ruler(options)).to.eql(new L.DG.Control.Ruler(options));
// 	});
// });