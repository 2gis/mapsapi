{#headAddress}
    <div class="dg-map-geoclicker-address-header">{.}</div>
{/headAddress}
{#address}
    <div class="dg-map-geoclicker-drilldown">{.}</div>
{/address}
<div class="dg-map-geoclicker-purpose">{purpose}</div>
<div class="dg-map-geoclicker-elevation">{elevation}</div>
