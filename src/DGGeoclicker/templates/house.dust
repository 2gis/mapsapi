{#headAddress}
    <div class="dg-map-geoclicker__address-header">{.}</div>
{/headAddress}
{#address}
    <div class="dg-map-geoclicker__drilldown">{.}</div>
{/address}
<div class="dg-map-geoclicker__purpose">{purpose}</div>
<div class="dg-map-geoclicker__elevation">{elevation}</div>
