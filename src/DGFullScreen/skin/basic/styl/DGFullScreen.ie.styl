/*ie7 fix width*/
.leaflet-right {
    *width : 40px;
}

.dg-fullscreen {
    zoom: 1;
    filter: unquote("alpha(opacity=80)");
}

.dg-fullscreen:hover {
    filter: unquote("alpha(opacity=100)");
}
